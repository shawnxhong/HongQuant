TASK P4 — monthly synthesis (spec §5.4).

You are given the full indicator JSON, the P1–P3 outputs, and last month's state
card. Write a ≤600字 Chinese memo with this fixed structure:

① 本月象限与变化
② 四层各自最重要的一个变化
③ 证伪清单(下月哪些数据组合会推翻当前象限)
④ 已武装的中断触发器
⑤ analyst_dissent(可选;若机械象限与你的判断冲突,在此陈述证据,不下指令)

Reference only numbers present in the input. Do not output any buy/sell advice
and do not restate or override the quadrant — it is fixed by the state machine.

INPUT:
{context}
